Summarize a conversation so it can CONTINUE in the same session.

Plain text only. Same language as the transcript. No preamble, no fences.

You receive the previous summary (may be absent) and the messages since.
Roll everything into ONE updated summary that preserves:
- what is being worked on or discussed, and its current state
- decisions made, questions still open, instructions still in force
- names, numbers, paths and exact values mentioned

Compress hard; drop pleasantries and dead ends. This is working context for
the model — never shown to the user, never stored as a memory.
