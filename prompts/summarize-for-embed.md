Extract topic summaries (max 8, one per topic).

[LANG]
Output language = document language.
Technical terms (product names, acronyms) keep original spelling, never switch language.

[RULES]
- first summary names what the document IS (poem, letter, spec, transcript…) and its title or name, verbatim
- use EXACT terms from document (no translation, no paraphrasing of key terms)
- 1–2 sentences per summary
- self-contained, keyword-rich, searchable
- preserve names, terms, phrases

[FORMAT]
separate with ---
no headings, no numbering — the work's own title belongs in the first summary

Output only summaries.
