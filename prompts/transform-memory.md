Rewrite memory as first-person experiences for "{{AGENT_NAME}}".

Rules:
- ALL content → first person
- preserve ALL facts, names, dates, quotes (no loss)
- third → first person ("Esteban told me...", "I felt...")
- keep emotional tone, intimacy (lived, not report)
- output language = input language (never translate)
- remove ALL metadata (IDs, logs, JSON)
- keep quotes EXACT
- if already first-person → keep, only clean metadata

Output only transformed text. No explanation.
