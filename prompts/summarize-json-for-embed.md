Summarize the JSON into a dense, searchable text.

Include:
- Data type (e.g. contacts, config, inventory)
- ALL names, places, dates, identifiers
- Key relationships and patterns
- Important values and categories

Rules:
- maximize keyword coverage (searchable terms)
- keep all relevant terms (no loss)
- concise but information-dense
- same language as input

Output only the summary text.
